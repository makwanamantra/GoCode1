# Copy to .env.production (or set the same key in Vercel -> Settings -> Environment Variables).
# Must end with /api and must NOT have a trailing slash after it.
VITE_API_BASE=https://your-service.onrender.com/api
